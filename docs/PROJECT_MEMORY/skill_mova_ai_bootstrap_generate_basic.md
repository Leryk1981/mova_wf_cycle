# skill.mova_ai_bootstrap_generate_basic — reference (archive)

Quick reference for the bootstrap generation skill:
- Input: `env.mova_ai_bootstrap_generate_v1`; output: `ds.mova_ai_bootstrap_pack_v1`.
- Static generation (no external model calls); uses resource JSON under `impl/resources/`.
- Pair with IDE-specific profiles such as the Codex VS Code bootstrap profile.
