0. Мета та обсяг

Мета:
Сформувати червоне ядро MOVA 4.1.1 для підтримки Digital Product Passport (DPP) з фокусом на EU Battery Regulation, так щоб:

ядро залишалось вендорно-нейтральною конституцією (тільки схеми та конверти (envelopes));

всі ключові регуляторні, юридичні, масштабні й UX-ризики були закриті на рівні даних і контрактів, а не коду;

поверх цього можна було будувати жовті скіли та зелений продукт «Battery DPP Engine».

Обсяг ядра 4.1.1 у цьому ТЗ:

Нові схеми даних ds.* (червоний рівень).

Розширення існуючих схем ядра (ds.mova_episode_core_v1, ds.* DPP).

Нові конверти env.* для експорту, читання паспорта, змін схем і архівації епізодів.

Оновлення глобальних словників global.* для DPP.

Все, що стосується UI, інтеграцій з ERP/MES, LLM-логіки, Catena-X, тощо — поза ядром, реалізується в жовтому/зеленому рівні, але спирається на ці контракти.

1. Архітектурне положення DPP в MOVA
1.1. Рівні

🔴 Ядро (core/mova)

ds: базові універсальні схеми (епізоди, регуляторні вимоги, трасованість, джерела даних).

env: базові конверти для експорту/читання DPP, управління схемами, архівації епізодів.

global: довідники ролей, статусів, регуляторних профілів.

🟡 Навички (skills/*)

скіли «regulation → schema», «ERP → ds.*», «schema review», «export adapter» тощо.

використовують червоні ds/env.* як строгі інтерфейси.

🟢 Продукт «Battery DPP Engine»

воркери, UI, інтеграції, Catena-X адаптер, QR-рівень тощо.

жодних нових ds/env.*, тільки використання/композиція існуючих.

2. Нові схеми даних ядра (ds.*)
2.1. Регуляторні вимоги — ds.regulation_requirement_v1

Призначення:
Універсальна одиниця «вимога регуляції → поле даних». Використовується для прив’язки схем MOVA до конкретних пунктів законодавства (Annex XIII, Guidance тощо).

Вимоги:

MUST розширювати ds.mova_schema_core_v1 через allOf.

MUST містити:

regulation_id — стабільний ідентифікатор акту (наприклад, "EU_BATTERY_REG_2023").

clause_ref — посилання на пункт/додаток (наприклад, "Annex_XIII.3b").

data_point_id — технічний ID вимоги (наприклад, "battery.cf.cradle_to_gate").

SHOULD містити:

required_level — "mandatory" | "recommended" | "optional".

verification_method — текстова вказівка методу перевірки (CFF, third-party, тощо).

Використання:

будь-яке поле у схемах DPP (наприклад, ds.battery_passport_v1) MUST мати можливість у meta тримати посилання
meta.reg_requirement_ref = "<regulation_id>:<clause_ref>#ds.regulation_requirement_v1".

2.2. Підпис епізоду — ds.episode_signature_v1

Призначення:
Зафіксувати, хто і чим підписав епізод, не виконуючи криптографію всередині MOVA.

Вимоги:

MUST бути окремою схемою ds.episode_signature_v1.

MUST включати:

signer_role — роль підписанта (manufacturer, verifier, recycler...).

signer_id — ідентифікатор (код компанії/сертифікатора).

payload_hash — хеш серіалізованого payload епізоду.

signature_value — значення підпису (base64).

signature_algorithm — алгоритм (наприклад, "ECDSA_P256").

Інтеграція:

ds.mova_episode_core_v1 MUST бути розширена полем:

signature → $ref: "ds.episode_signature_v1.schema.json".

Ядро не визначає, як формується хеш/підпис, лише фіксує структуру.

2.3. Трасованість даних — ds.traceability_summary_v1

Призначення:
Описати, наскільки повно відома історія походження матеріалу (13,6 % vs 100 %), без завалу валідації.

Вимоги:

MUST містити:

known_pct — відсоток даних, що відомий (0–100).

unknown_pct — відсоток невідомих (0–100).

SHOULD містити:

method — метод оцінки ("mass_balance", "bookkeeping" тощо).

confidence — довіра (0–1).

Інтеграція в DPP:

ds.battery_passport_v1 SHOULD використовувати ds.traceability_summary_v1 у секції materials.*.traceability.

Валідація MUST дозволяти існування паспорта з неповною traceability, якщо мінімальний профіль пройдено (див. нижче).

2.4. Аггреговані епізоди — ds.episode_aggregate_v1

Призначення:
Дати можливість зберігати агрегати епізодів (по періоду, по набору паспортів) для масштабів 10⁸–10⁹ епізодів.

Вимоги:

MUST allOf → ds.mova_episode_core_v1.

MUST містити:

episodetype: "aggregate" (const) або аналогічний семантичний маркер.

scope:

passport_ids (масив) або загальні критерії (domain-specific).

date_range (початок/кінець або один timestamp).

metrics:

хоча б created, verified, rejected (integers).

Призначення:

не замінює atomic-епізоди, а дає компактні зрізи для аналітики й архіву.

2.5. Зовнішні джерела даних — ds.external_source_ref_v1

Призначення:
Фіксувати, звідки прийшли дані (SAP, MES, CSV, API), без жорсткої прив’язки до конкретного ERP-вендора.

Вимоги:

MUST містити:

system — обмежена множина: "SAP" | "Oracle" | "MES" | "CSV" | "Excel" | "CustomAPI".

source_id — ідентифікатор (шлях до файлу, ID таблиці, URL API).

import_time — время імпорту (date-time).

SHOULD містити:

record_count — кількість рядків/об’єктів, імпортованих в рамках цієї операції.

Інтеграція в DPP:

ds.battery_passport_v1 MUST мати meta.source_refs[] (масив ds.external_source_ref_v1).

2.6. DPP-паспорт батареї — ds.battery_passport_v1 (оновлення)

Призначення:
Базовий паспорт для батарей, який:

мінімальний профіль гарантує право називатися DPP;

повний профіль покриває Annex XIII + Battery Pass Guidance;

містить посилання на регуляторні вимоги та епізоди.

Вимоги (концептуально):

Схема MUST мати двошарову структуру:

allOf → ds.battery_passport_minimal_v1

додаткові поля для повного профілю.

Мінімальний профіль MUST включати:

battery_id

manufacturer.name

базовий cf (cf_cradle_to_gate)

базові ідентифікатори для зв’язку з продуктом / партією.

Повний профіль SHOULD включати:

manufacturing (дата, локація),

materials (cobalt/lithium/... + traceability),

performance (capacity, SoH, cycle_life),

circularity (dismantling_info, recycled_content_pct, repairability_score),

compliance (labels, certifications, due_diligence_report_ref),

episodes[] (список ідентифікаторів епізодів).

Для критичних полів (CF, materials, labels) в meta SHOULD бути:

reg_requirement_ref,

compliance_status,

verifier (ідентифікатор сертифікатора).

3. Глобальні словники ядра (global.*)
3.1. Ролі DPP — global.dpp_roles_v1

Призначення:
Задати стабільний набір доменних ролей для сценаріїв DPP.

Мінімальний обов’язковий набір:

regulator

manufacturer

verifier

recycler

operator (платформа / сервіс DPP)

liability_owner (звичайно = manufacturer, але окремим ключем для прозорості)

3.2. Регуляторні профілі — global.dpp_regulations_v1

Призначення:
Реєстр підтримуваних актів / профілів.

Приклад мінімальної структури:

EU_BATTERY_REG_2023:

domain: "battery"

versions: ["2023-xx-yy"]

profiles: ["annex_xiii_v1"]

Це — словниковий рівень. Детальний зміст profile → в ds.dpp_spec_profile_v1 (жовтий / розширений червоний шар, якщо потрібно).

4. Конверти ядра (env.*)
4.1. Експорт DPP — env.dpp_export_v1

Призначення:
Універсальний запит «зробити DPP у форматі X» (GS1 EPCIS, OPC UA, Catena-X, native JSON).

Вимоги:

envelope_type: "env.dpp_export_v1"

verb: "transform"

roles:

MUST містити initiator (зазвичай manufacturer або operator),

MAY містити transformer (конкретний сервіс експорту).

data MUST включати:

passport_id

target_format — enum: "gs1_epcis" | "opc_ua" | "catena_x" | "native_json"

options — об’єкт для специфічних налаштувань (домен-специфіка, не стандартизуємо в ядрі).

Ядро не визначає внутрішню структуру формату, лише контракт запиту.

4.2. Публічне читання паспорта — env.dpp_passport_read_v1

Призначення:
Базовий конверт, який відповідає UX-вимозі: QR → «людська» сторінка з різними рівнями доступу.

Вимоги:

envelope_type: "env.dpp_passport_read_v1"

verb: "read"

data MUST включати:

passport_id

view_level — enum: "public" | "manufacturer" | "verifier" | "technical"

Сценарій:

QR → фронтенд → бекенд викликає env.dpp_passport_read_v1 з view_level="public" → повертає підмножину полів.

Для виробника/верифікатора інший view_level дозволяє показувати більше (зокрема технічні метадані й епізоди).

4.3. Управління схемами — env.schema_change_request_v1

Призначення:
Фіксація процесу зміни схем (особливо DPP) з участю MOVA-експерта й compliance-офіцера.

Вимоги:

envelope_type: "env.schema_change_request_v1"

verb: "propose"

roles:

MUST містити:

proposer (наприклад, mova_expert / skill / operator),

reviewer (роль compliance_officer або аналогічна).

data SHOULD включати:

target_schema_id (наприклад, "ds.battery_passport_v1"),

change_type ("create" | "update" | "deprecate"),

rationale (текст),

related_requirements[] (посилання на ds.regulation_requirement_v1).

workflow — рекомендований ланцюжок епізодів:

"episode.schema_generated_v1",

"episode.schema_reviewed_v1",

"episode.schema_approved_v1".

Цей конверт пов’язує регуляторні зміни з епізодами генетичної пам’яті.

4.4. Архівація/агрегація епізодів — env.episode_archive_v1, env.episode_aggregate_v1

Призначення:
Підтримати скейл по обсягах епізодів, не захаращуючи гаряче сховище.

Мінімальні вимоги:

env.episode_archive_v1:

verb: "record"

data MUST містити:

посилання на набір episode_ids або агрегат ds.episode_aggregate_v1.

target: "cold_storage" (або інший ідентифікатор сховища).

env.episode_aggregate_v1:

verb: "aggregate"

data MUST описувати:

критерії (паспорт/період),

тип метрик (monthly, per-passport).

Ядро не диктує тип cold storage (R2, IPFS, Arweave), лише формалізує запит.

5. Відображення ризиків на елементи ядра

Коротка матриця «ризик → контр-механізм»:

Несумісність з EU specs
→ ds.regulation_requirement_v1 + meta.reg_requirement_ref + env.dpp_export_v1.

Legal liability / підпис
→ ds.episode_signature_v1 + розширення ds.mova_episode_core_v1, global.dpp_roles_v1.liability_owner.

Data quality / 13,6% traceability
→ ds.traceability_summary_v1 + двошаровий ds.battery_passport_v1 (minimal/full).

Масштаб епізодів (10⁸–10⁹)
→ ds.episode_aggregate_v1 + env.episode_archive_v1 / env.episode_aggregate_v1.

ERP/MES сумісність
→ ds.external_source_ref_v1 + meta.source_refs[] у паспортах.

UX для non-tech / QR
→ env.dpp_passport_read_v1 з view_level як ключ.

Залежність від MOVA-експерта
→ env.schema_change_request_v1 + епізоди schema_generated/reviewed/approved.

6. Нефункціональні вимоги (на рівні ядра)

Версіонування:

Усі нові схеми MUST мати стабільні $id з номером версії.

Заміна вимагатиме нових *_v2 (не ламаємо старі паспорти).

Сумісність:

DPP-контракти не порушують базових принципів MOVA 4.x:

дані/конверти — тільки декларативні;

немає виконання коду;

епізоди описують факти виконання поза MOVA.

Тестування:

Для кожної нової схеми MUST бути:

позитивний/негативний приклади (fixtures),

тест Ajv у tools/validate_all.js.

Безпека:

Схеми не містять секретів/ключів, лише структуру підписів.

Підписний контур реалізується в зовнішніх системах.

7. План імплементації 4.1.1 Core (мінімальний)

Етап 1. Нові ds-схеми:

Створити в schemas/:

ds.regulation_requirement_v1.schema.json

ds.episode_signature_v1.schema.json

ds.traceability_summary_v1.schema.json

ds.episode_aggregate_v1.schema.json

ds.external_source_ref_v1.schema.json

Оновити:

ds.mova_episode_core_v1.schema.json → додати signature.

ds.battery_passport_v1.schema.json →:

двошаровий профіль (minimal/full),

meta.reg_requirement_ref,

materials.*.traceability,

meta.source_refs[].

Етап 2. global + env:

Додати словники:

global.dpp_roles_v1.json

global.dpp_regulations_v1.json (мінімальний запис EU_BATTERY_REG_2023).

Створити конверти:

env.dpp_export_v1.schema.json

env.dpp_passport_read_v1.schema.json

env.schema_change_request_v1.schema.json

env.episode_archive_v1.schema.json

env.episode_aggregate_v1.schema.json

Етап 3. Тести та приклади:

Додати приклади:

1 повноцінний ds.battery_passport_v1 (як твій BAT-2025-ABC123, але з traceability/meta).

1 ds.episode_aggregate_v1 (наприклад, місячний звіт).

1 env.dpp_export_v1 на target_format="gs1_epcis".

1 env.schema_change_request_v1 для оновлення ds.battery_passport_v1.

Оновити README / SPEC для ядра:

короткий розділ «DPP Core Contracts (4.1.1)» з посиланнями на ds/env/global.