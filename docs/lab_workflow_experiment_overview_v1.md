# MOVA Skills Lab: Лабораторія воркфлоу v1

**Дата:** 2025-12-10  
**Проєкт:** mova_skills_lab  
**Фокус:** еволюція воркфлоу через конкуренцію моделей та генетичний шар

---

## 1. Ідея лабораторії

Лабораторія воркфлоу в MOVA Skills Lab — це місце, де:

- реальні робочі процеси бізнесу описуються у вигляді формальних процедур (procedure) мовою MOVA;
- кілька моделей / агентів отримують однакову вихідну гіпотезу (as-is процес) і генерують альтернативні варіанти воркфлоу;
- кожен варіант проганяється на реальних або контрольованих кейсах;
- результати запусків фіксуються як епізоди (episodes) генетичного шару;
- на основі епізодів формується поліпшений воркфлоу, який можна повторно використовувати.

Ключова установка: **MOVA не виконує код і не “вирішує”, який воркфлоу кращий**. Вона задає контракт, фіксує структуру процесів та епізодів, а вибір і еволюція робляться на основі даних.

---

## 2. Базові сутності лабораторії

У лабораторії фігурують чотири основні види об’єктів:

1. **Процедура воркфлоу (Procedure)**  
   Описує конкретний процес у бізнесі чи в розробці (наприклад, “repo snapshot → change plan → apply step”).  
   - Має версію, список кроків, ролі, вхідні/вихідні дані.

2. **Конфігурація експерименту (Experiment Config)**  
   Описує, як ми порівнюємо варіанти воркфлоу:  
   - який базовий процес беремо за відправну точку (as-is);
   - які моделі / агенти генерують кандидати;
   - які метрики оцінювання важливі (час, помилки, гроші, NPS тощо);
   - які обмеження (stability, безпека, незмінні кроки).

3. **Експеримент і варіанти (Variants & Runs)**  
   - Для кожної моделі створюється варіант процедури (candidate procedure).  
   - Кожен варіант запускається на наборі кейсів, утворюючи епізоди виконання.

4. **Результати експерименту (Experiment Result)**  
   - Збираються всі епізоди;  
   - Обчислюються агреговані метрики по кожному варіанту;  
   - Формується висновок: який варіант кращий і чому, які патерни варто зафіксувати в наступній версії воркфлоу.

---

## 3. Ролі та шари MOVA

Лабораторія спирається на звичні рівні MOVA:

- 🔴 **Червоний шар (core/mova):**  
  - Базові схеми епізодів, глобальні словники ролей/ресурсів;  
  - Фіксація того, що таке episode, actor, tool, metric.

- 🟡 **Жовтий шар (skills/*):**  
  - Скіли для планування, генерації варіантів, запуску процедур і агрегації результатів;  
  - Стандартні конверти (envelopes) для “запусти експеримент”, “згенеруй варіант”, “збери результати”.

- 🟢 **Зелений шар (lab/*):**  
  - Конкретні воркфлоу реальних проєктів (DPP-lab, Smartlink, file_cleanup, repo_change_plan тощо);  
  - Експериментальні конфігурації, приклади, хронологія запусків.

Кожен запуск експерименту → **генетичний епізод**, який можна зберігати разом з іншими епізодами Skills Lab.

---

## 4. Участь моделей / агентів

Кілька різних моделей працюють як “конкурентні дизайнери воркфлоу”:

- ChatGPT (chat UI / browser);
- Codex / Qwen / Gemini в IDE (repo-агенти);
- інші external-агенти, інтегровані через коннектори.

Для кожної моделі ми фіксуємо:

- `agent_id` / `model_family` (gpt, gemini, qwen…);
- `profile_id` (наприклад, `chatgpt_main`, `codex_vscode_main`);
- контекст запуску (chat, IDE, worker).

Це дозволяє аналізувати не лише воркфлоу, а й **поведінку різних агентських оболонок** поверх однієї мови.

---

## 5. Потік роботи лабораторії

Узагальнений цикл лабораторії воркфлоу:

1. **Описати as-is воркфлоу** у вигляді процедури MOVA (lab_workflow_procedure).  
2. **Сконфігурувати експеримент**: цілі, метрики, список агентів/моделей.  
3. **Згенерувати варіанти воркфлоу** від різних моделей.  
4. **Запустити кожен варіант** на контрольованих кейсах.  
5. **Зібрати епізоди** виконання й агрегувати метрики.  
6. **Порівняти варіанти** і сформувати поліпшену версію воркфлоу.  
7. **Зафіксувати результат** як нову процедуру + епізод результату експерименту.

На кожному кроці дані зберігаються у вигляді JSON, валідованого схемами MOVA. Це дозволяє:

- повторювати експерименти;
- відстежувати еволюцію процесів;
- будувати аналітику по воркфлоу та по моделях.

---

## 6. Межі модернізації

Ця модернізація лабораторії обмежується:

- додаванням **lab-схем та конвертів** для воркфлоу-експериментів;
- додаванням **жовтих скілів** для планування / генерації / запуску / агрегації;
- описом одного **пілотного патерну** запуску (перший реальний кейс);
- дорожньою картою реалізації по кроках.

Міграція ядра MOVA та зміни у червоному шарі — поза межами цього ТЗ і робитимуться окремо.

---

## 7. Standard domain experiment pattern (lab)
Для доменних воркфлоу (SmartLink, Social Pack, DPP-модулі та інші) ми застосовуємо уніфікований патерн одного проходу лабораторії: створюємо MOVA-пак кандидата, ставимо експеримент baseline vs MOVA (WF-EX-*), виконуємо перший прогін, фіксуємо вердикт і наступні дії, а також знімаємо снапшот. Деталізований чекліст описано в `docs/PROJECT_MEMORY/lab_workflow_pattern_domain_experiment_v1.md` і використовується як стандартний сценарій для нових доменів.
