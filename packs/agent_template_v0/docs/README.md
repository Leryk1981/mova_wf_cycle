# Agent Template Pack v0

Bundles a minimal Claude Code operator template that includes MOVA policy, registry, roles, and pipeline artifacts.
Use `packs/agent_template_v0/tools/agent_template_generate_v0.mjs` to generate the bundle, then verify via `npm run quality:agent_template` or the negative suite described in `docs/agent_template_negative_suite_v0.json`.
