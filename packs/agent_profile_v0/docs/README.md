# Agent Template Pack v0

Bundles a minimal Claude Code operator template that includes MOVA policy, registry, roles, and pipeline artifacts.
Use `packs/agent_profile_v0/tools/agent_profile_generate_v0.mjs` to generate the bundle, then verify via `npm run quality:agent_profile` or the negative suite described in `docs/agent_profile_negative_suite_v0.json`.
