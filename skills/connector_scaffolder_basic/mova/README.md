# MOVA overlays for `skill.connector_scaffolder_basic`

This folder contains local MOVA definitions for the skill:

- `ds/` – local data schemas:
  - `ds.connector_scaffold_request_v1.schema.json` – input schema describing connector requirements and API docs bundle
  - `ds.connector_scaffold_result_v1.schema.json` – output schema with complete file plan for the connector

- `env/` – local envelopes:
  - `env.connector_scaffold_run_v1.schema.json` – envelope for running the connector scaffolder skill

