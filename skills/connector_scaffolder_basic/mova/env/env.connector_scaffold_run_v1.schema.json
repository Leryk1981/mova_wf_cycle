{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/env.connector_scaffold_run_v1.schema.json",
  "title": "Connector Scaffold Run Envelope v1",
  "description": "Envelope for transforming an API description and docs bundle into a complete MOVA connector skill scaffold.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_id",
    "verb",
    "resource",
    "input"
  ],
  "properties": {
    "envelope_id": {
      "type": "string",
      "const": "env.connector_scaffold_run_v1",
      "description": "Identifier of this envelope type."
    },
    "verb": {
      "type": "string",
      "const": "transform",
      "description": "Verb describing what this envelope does."
    },
    "resource": {
      "type": "string",
      "const": "skill",
      "description": "Resource type: we transform an API description into a skill scaffold."
    },
    "input": {
      "$ref": "https://mova.dev/schemas/ds.connector_scaffold_request_v1.schema.json",
      "description": "API description and documentation bundle for the connector."
    },
    "output": {
      "$ref": "https://mova.dev/schemas/ds.connector_scaffold_result_v1.schema.json",
      "description": "Complete file plan for the new connector skill."
    },
    "meta": {
      "type": "object",
      "description": "Optional metadata about this run (tags, language, etc.).",
      "additionalProperties": true
    }
  }
}

