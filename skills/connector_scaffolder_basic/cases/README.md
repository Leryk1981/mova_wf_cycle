# Cases for `skill.connector_scaffolder_basic`

This folder contains example cases for the skill, demonstrating:

- given a connector description (vendor, service, operations) and API docs bundle,
- what is the expected file plan with all generated files.

Each `*.json` file here documents one such input/output pair.

