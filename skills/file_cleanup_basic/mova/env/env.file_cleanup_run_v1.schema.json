{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/env.file_cleanup_run_v1.schema.json",
  "title": "File Cleanup Run Envelope v1",
  "description": "Envelope for transforming a disk snapshot into a structured cleanup plan.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_id",
    "verb",
    "resource",
    "input"
  ],
  "properties": {
    "envelope_id": {
      "type": "string",
      "const": "env.file_cleanup_run_v1",
      "description": "Identifier of this envelope type."
    },
    "verb": {
      "type": "string",
      "const": "transform",
      "description": "Verb describing what this envelope does."
    },
    "resource": {
      "type": "string",
      "const": "note",
      "description": "Resource type: we transform a disk snapshot into a structured cleanup plan."
    },
    "input": {
      "$ref": "https://mova.dev/schemas/ds.file_cleanup_request_v1.schema.json",
      "description": "Disk snapshot and user preferences for cleanup."
    },
    "output": {
      "$ref": "https://mova.dev/schemas/ds.file_cleanup_result_v1.schema.json",
      "description": "Structured cleanup plan produced from the input."
    },
    "meta": {
      "type": "object",
      "description": "Optional metadata about this run (tags, language, etc.).",
      "additionalProperties": true
    }
  }
}

