# Cases for `skill.repo_snapshot_basic`

This folder contains example cases for the skill, demonstrating:

- given raw repository data (structure dump, README, notes),
- what is the expected structured output (snapshot markdown, memory summary, checklist).

Each `*.json` file here documents one such input/output pair.

