{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/env.repo_change_plan_run_v1.schema.json",
  "title": "Repository Code Change Plan Run Envelope v1",
  "description": "Envelope for generating a structured plan of code changes in a repository based on a snapshot and goal description.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_id",
    "verb",
    "resource",
    "input"
  ],
  "properties": {
    "envelope_id": {
      "type": "string",
      "const": "env.repo_change_plan_run_v1",
      "description": "Identifier of this envelope type."
    },
    "verb": {
      "type": "string",
      "const": "transform",
      "description": "Verb: transform repository context and goal into a change plan."
    },
    "resource": {
      "type": "string",
      "const": "code_change_plan",
      "description": "Resource type: a code change plan."
    },
    "input": {
      "$ref": "https://mova.dev/schemas/ds.repo_change_plan_request_v1.schema.json",
      "description": "Repository snapshot, goal summary, and constraints."
    },
    "output": {
      "$ref": "https://mova.dev/schemas/ds.repo_change_plan_result_v1.schema.json",
      "description": "Structured, step-by-step plan of code changes."
    },
    "meta": {
      "type": "object",
      "description": "Optional metadata about this planning run (tags, caller, etc.).",
      "additionalProperties": true
    }
  }
}

