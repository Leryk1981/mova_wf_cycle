# Cases for `skill.repo_code_change_plan_basic`

This folder contains example cases for the skill, demonstrating:

- given a repository snapshot, goal description, and constraints,
- what is the expected structured change plan with steps, risks, and completion criteria.

Each `*.json` file here documents one such input/output pair.

