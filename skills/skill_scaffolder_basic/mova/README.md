# MOVA overlays for `skill.skill_scaffolder_basic`

This folder contains local MOVA definitions for the skill:

- `ds/` – local data schemas:
  - `ds.skill_scaffold_request_v1.schema.json` – input schema describing a high-level skill request
  - `ds.skill_scaffold_result_v1.schema.json` – output schema with the complete file plan

- `env/` – local envelopes:
  - `env.skill_scaffold_run_v1.schema.json` – envelope for running the scaffolder skill

