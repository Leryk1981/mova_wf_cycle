{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/env.skill_scaffold_run_v1.schema.json",
  "title": "Skill Scaffold Run Envelope v1",
  "description": "Envelope for transforming a high-level skill description into a complete file plan.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_id",
    "verb",
    "resource",
    "input"
  ],
  "properties": {
    "envelope_id": {
      "type": "string",
      "const": "env.skill_scaffold_run_v1",
      "description": "Identifier of this envelope type."
    },
    "verb": {
      "type": "string",
      "const": "transform",
      "description": "Verb describing what this envelope does."
    },
    "resource": {
      "type": "string",
      "const": "note",
      "description": "Resource type: we transform a skill description into a structured file plan."
    },
    "input": {
      "$ref": "https://mova.dev/schemas/ds.skill_scaffold_request_v1.schema.json",
      "description": "High-level request for creating a new skill."
    },
    "output": {
      "$ref": "https://mova.dev/schemas/ds.skill_scaffold_result_v1.schema.json",
      "description": "Complete file plan for the new skill."
    },
    "meta": {
      "type": "object",
      "description": "Optional metadata about this run (tags, language, etc.).",
      "additionalProperties": true
    }
  }
}

