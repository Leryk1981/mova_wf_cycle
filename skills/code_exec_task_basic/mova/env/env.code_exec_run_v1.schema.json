{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/env.code_exec_run_v1.schema.json",
  "title": "Code Execution Run Envelope v1",
  "description": "Envelope for executing a single CLI command in a repository and returning a standardized result.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_id",
    "verb",
    "resource",
    "input"
  ],
  "properties": {
    "envelope_id": {
      "type": "string",
      "const": "env.code_exec_run_v1",
      "description": "Identifier of this envelope type."
    },
    "verb": {
      "type": "string",
      "const": "transform",
      "description": "Verb: transform a command request into an execution result."
    },
    "resource": {
      "type": "string",
      "const": "code_exec_task",
      "description": "Resource type: a code execution task."
    },
    "input": {
      "$ref": "https://mova.dev/schemas/ds.code_exec_request_v1.schema.json",
      "description": "Command to execute with working directory, arguments, timeout, and other parameters."
    },
    "output": {
      "$ref": "https://mova.dev/schemas/ds.code_exec_result_v1.schema.json",
      "description": "Execution result with exit code, logs, duration, and summary."
    },
    "meta": {
      "type": "object",
      "description": "Optional metadata about this execution (tags, caller, etc.).",
      "additionalProperties": true
    }
  }
}

