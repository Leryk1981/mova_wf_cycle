# Cases for `skill.code_exec_task_basic`

This folder contains example cases for the skill, demonstrating:

- given a command request (working directory, command arguments, timeout),
- what is the expected execution result with formatted summary.

Each `*.json` file here documents one such input/output pair.

