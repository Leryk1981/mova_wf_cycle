# Cases for `skill.runtime_binding_code_scaffolder_basic`

This folder contains example cases for the skill, demonstrating:

- given a skill/envelope description and runtime requirements,
- what is the expected code skeleton with all generated files.

Each `*.json` file here documents one such input/output pair.

