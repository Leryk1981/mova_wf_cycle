{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/env.runtime_binding_scaffold_run_v1.schema.json",
  "title": "Runtime Binding Scaffold Run Envelope v1",
  "description": "Envelope for transforming a skill/envelope description and runtime requirements into code skeleton files.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_id",
    "verb",
    "resource",
    "input"
  ],
  "properties": {
    "envelope_id": {
      "type": "string",
      "const": "env.runtime_binding_scaffold_run_v1",
      "description": "Identifier of this envelope type."
    },
    "verb": {
      "type": "string",
      "const": "transform",
      "description": "Verb describing what this envelope does."
    },
    "resource": {
      "type": "string",
      "const": "runtime_binding",
      "description": "Resource type: we transform skill/envelope descriptions into runtime binding code."
    },
    "input": {
      "$ref": "https://mova.dev/schemas/ds.runtime_binding_scaffold_request_v1.schema.json",
      "description": "Skill/envelope description and runtime requirements."
    },
    "output": {
      "$ref": "https://mova.dev/schemas/ds.runtime_binding_scaffold_result_v1.schema.json",
      "description": "Code skeleton files and optional binding JSON files."
    },
    "meta": {
      "type": "object",
      "description": "Optional metadata about this run (tags, language, etc.).",
      "additionalProperties": true
    }
  }
}

