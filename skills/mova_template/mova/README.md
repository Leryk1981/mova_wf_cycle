# MOVA overlays for `skill.mova_template`

This folder will contain local MOVA definitions for the skill:

- `ds/` – local data schemas (e.g. a structured procedure object).

- `env/` – local envelopes (e.g. `env.mova_template_run_v1`).

In this bootstrap step we only create the structure. Concrete schemas

will be added in a later task.

