# skill.mova_ai_bootstrap_generate_basic

Baseline yellow skill for generating a MOVA AI bootstrap pack.
- Input: `env.mova_ai_bootstrap_generate_v1` (target model/environment description).
- Output: `ds.mova_ai_bootstrap_pack_v1` (static JSON with instructions, rules, recipes, examples).
- Does not call external models or mutate the repository; uses a static resource JSON.
