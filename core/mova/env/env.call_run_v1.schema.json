{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/core/mova/env/env.call_run_v1.schema.json",
  "title": "Call Run Envelope v1",
  "description": "Red-zone envelope: second-order speech act that invokes another envelope. This is a foundational primitive for building scenarios/recipes, not a skill-specific envelope.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "envelope_id",
    "verb",
    "resource",
    "input"
  ],
  "properties": {
    "envelope_id": {
      "type": "string",
      "const": "env.call_run_v1",
      "description": "Identifier of this envelope type."
    },
    "verb": {
      "type": "string",
      "const": "call",
      "description": "Verb describing what this envelope does: call another envelope."
    },
    "resource": {
      "type": "string",
      "const": "envelope",
      "description": "Resource type: we are calling another envelope."
    },
    "input": {
      "$ref": "https://mova.dev/schemas/core/mova/ds/ds.call_request_v1.schema.json",
      "description": "Request to invoke a target envelope."
    },
    "output": {
      "$ref": "https://mova.dev/schemas/core/mova/ds/ds.call_result_v1.schema.json",
      "description": "Result of invoking the target envelope."
    },
    "meta": {
      "type": "object",
      "description": "Optional metadata about this call (tags, language, etc.).",
      "additionalProperties": true
    }
  }
}

