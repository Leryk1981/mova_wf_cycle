{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mova.dev/schemas/core/mova/env/env.skill_ingest_run_request_v1.schema.json",
  "title": "Envelope: Skill Ingest Run Request v1",
  "description": "Speech act: request to run a Skill Seeker ingest pipeline with given source and run configs.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "mova_version",
    "envelope_type",
    "envelope_id",
    "skill_ingest_source_config",
    "skill_ingest_run_config"
  ],
  "properties": {
    "mova_version": {
      "type": "string",
      "const": "4.0.0",
      "description": "MOVA core version targeted by this envelope."
    },
    "envelope_type": {
      "type": "string",
      "const": "env.skill_ingest_run_request_v1",
      "description": "Logical identifier of this envelope kind."
    },
    "envelope_id": {
      "type": "string",
      "description": "Unique id of this envelope instance (referenced from episodes)."
    },
    "requested_by": {
      "type": "string",
      "description": "Initiator id (human, agent, or scenario)."
    },
    "requested_at": {
      "type": "string",
      "format": "date-time",
      "description": "When this request was created."
    },
    "priority": {
      "type": "string",
      "description": "Optional priority hint for the executor (e.g. low, normal, high)."
    },
    "skill_ingest_source_config": {
      "$ref": "https://mova.dev/schemas/core/mova/ds/ds.skill_ingest_source_config_v1.schema.json",
      "description": "Inline source configuration describing where to ingest from."
    },
    "skill_ingest_run_config": {
      "$ref": "https://mova.dev/schemas/core/mova/ds/ds.skill_ingest_run_config_v1.schema.json",
      "description": "Inline run configuration describing how to execute the ingest pipeline."
    }
  }
}
